{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}

    <div class="hero">
        <div class="hero-text">
                <h2><a href="{{ path('app_devis_new') }}" class="btn btn-warning">Demander un devis</a></h2>
        </div>
    </div>
    <div>
        {#        Si l'utilisateur est connecté #}
        {% if app.user %}
            <h3><a href="{{ path('app_devis_index') }}" class="btn btn-info">Liste des devis</a></h3>
            <h3><a href="{{ path('app_livraison_index') }}" class="btn btn-info">Liste des livraisons</a></h3>
            <h3><a href="{{ path('app_user_index') }}" class="btn btn-info">Liste des utilisateurs</a></h3>
        {% endif %}
    </div>

    <div>
        <a href="{{ path('app_expediteur_index') }}" class="btn btn-secondary">Expéditeurs</a><br>
        <a href="{{ path('app_expediteur_new') }}" class="btn btn-secondary">Créer un expéditeur</a><br>
        <a href="{{ path('app_destinataire_index') }}" class="btn btn-secondary">Destinataires</a><br>
        <a href="{{ path('app_destinataire_new') }}" class="btn btn-secondary">Créer un destinataire</a><br>
        <a href="{{ path('app_marchandise_index') }}" class="btn btn-secondary">Marchandises</a><br>
        <a href="{{ path('app_marchandise_new') }}" class="btn btn-secondary">Créer une marchandise</a>
    </div>

{% endblock %}
