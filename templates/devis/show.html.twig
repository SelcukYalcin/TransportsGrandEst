{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N° de Devis :</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>Date de validité :</th>
                <td>{{ devi.dateVal ? devi.dateVal|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_index') }}">Retour à la liste des devis</a><br/>

    <a href="{{ path('app_devis_edit', {'id': devi.id}) }}">Modifier</a>

    {{ include('devis/_delete_form.html.twig') }}
{% endblock %}
